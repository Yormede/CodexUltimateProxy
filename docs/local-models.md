# Local models

Defaults:

- LM Studio: `http://127.0.0.1:1234/v1`
- Ollama: `http://127.0.0.1:11434/v1`

OmniCodex does not scan the LAN. Add other local endpoints explicitly in
`omnicodex.config.json`, then regenerate the Codex catalog.
