services:
  omnicodex:
    build: .
    ports:
      - "127.0.0.1:4141:4141"
    environment:
      OMNICODEX_HOST: 0.0.0.0
      OMNICODEX_ALLOW_REMOTE: "1"
      DEEPSEEK_API_KEY: ${DEEPSEEK_API_KEY:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
